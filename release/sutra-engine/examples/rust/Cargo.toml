[package]
name = "sutra-engine-rust-demo"
version = "0.1.0"
edition = "2026"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rmp-serde = "1.1"

[workspace]
# Empty workspace to ensure this is standalone

